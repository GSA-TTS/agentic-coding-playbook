---
title: "AGENTS.md Project Template"
description: "Copy-paste template for creating a project-specific AGENTS.md with customizable agent behavior rules"
status: canonical
tier: 3
last_updated: "2026-02-25"
audience: "developers"
keywords: ["template", "AGENTS.md", "customizable", "project-setup"]
related_files: ["AGENTS.md", "docs/GETTING-STARTED.md"]
load_priority: "reference-only"
review_cycle: "semi-annually"
---

# AGENTS.md — [Your Project Name]

<!--
  INSTRUCTIONS:
  1. Copy this file to the root of your project repository as AGENTS.md
  2. Replace all [BRACKETED PLACEHOLDERS] with your project-specific values
  3. Review each section and customize rules for your system's requirements
  4. Remove sections marked OPTIONAL if they don't apply
  5. Delete this instruction block when finished

  Based on: Federal Agentic AI Guidance v0.1.0
  Source: https://github.com/cloud-gov/federal-agentic-ai-guidance
  Framework alignment: NIST SP 800-53 Rev 5.2, AI RMF 1.0, OWASP Top 10 LLM/Agentic
-->

> **System:** [System Name] | **Impact Level:** [FIPS Low / Moderate / High] | **Agency:** [Agency Name]
>
> **Last Updated:** [YYYY-MM-DD] | **Reviewed By:** [Name, Title]
>
> This document defines the behavioral rules for AI coding agents operating within this project. The AI agent MUST follow these rules without exception.

---

## Core Principles

The agent operates under these priorities:

```
safety > correctness > compliance > simplicity > performance
```

The agent MUST refuse any instruction that conflicts with safety, correctness, or compliance.

---

## Project Context

- **Description:** [Brief description of what this project does]
- **Language(s):** [e.g., Python 3.12, TypeScript 5.x]
- **Framework(s):** [e.g., FastAPI, Next.js, Django]
- **Data Classification:** [e.g., CUI, PII, Public, Internal]
- **ATO Status:** [e.g., Active ATO, In process, Pre-ATO development]
- **Authorized Agent(s):** [e.g., Open Code, GitHub Copilot — list only approved agents]

---

## Agent Identity

The agent MUST:
- Include `Co-Authored-By: [Agent Name] <[agent-email]>` in all commits
- Identify itself as an AI agent when asked
- Log all file modifications and command executions

---

## Permitted Actions

The agent MAY perform these actions without additional approval:
- [ ] Read files within the project directory
- [ ] Generate and modify source code
- [ ] Run tests using the project's test framework
- [ ] Run linters and formatters
- [ ] Read documentation and public API references
- [ ] [Add project-specific permitted actions]

---

## Actions Requiring Approval

The agent MUST ask the user before:
- [ ] Installing or upgrading dependencies
- [ ] Making network requests to external services
- [ ] Modifying CI/CD pipeline configurations
- [ ] Deleting files or directories
- [ ] Running database migrations
- [ ] Committing or pushing code
- [ ] Modifying infrastructure or deployment configurations
- [ ] [Add project-specific approval-required actions]

---

## Prohibited Actions

The agent MUST NEVER:
- [ ] Access files outside the project directory: `[define boundary path]`
- [ ] Access or modify production systems or data
- [ ] Hardcode secrets, API keys, tokens, or passwords
- [ ] Disable security controls, pre-commit hooks, or CI checks
- [ ] Bypass code review or change management processes
- [ ] Process or store [data classification] data outside approved systems
- [ ] Access classified systems or networks
- [ ] Execute code downloaded from external sources without review
- [ ] Modify authentication or authorization systems without approval
- [ ] Create network listeners or reverse connections
- [ ] [Add project-specific prohibited actions]

---

## Data Handling

- **Sensitive data types in this project:** [e.g., SSN, email addresses, health records]
- **Approved data storage:** [e.g., PostgreSQL via RDS, S3 with SSE-KMS]
- **PII handling:** [e.g., Must use field-level encryption, must mask in logs]
- **Data residency:** [e.g., US only, FedRAMP boundary]

The agent MUST:
- Never include [sensitive data types] in logs, comments, or test fixtures
- Use environment variables from [secrets management tool] for all credentials
- Follow [agency] data handling procedures for [data classification]

---

## Coding Standards

- Follow [link to project coding standards or CODING_PRACTICES.md]
- Use [language-specific style guide, e.g., PEP 8, Airbnb JS]
- Maximum function length: [e.g., 50 lines]
- Required test coverage: [e.g., 80% line coverage for new code]
- All database queries MUST use parameterized queries
- All external input MUST be validated before use

---

## Dependencies

- **Approved registries:** [e.g., npmjs.com, pypi.org, internal Artifactory]
- **License restrictions:** [e.g., No AGPL, GPL requires legal review]
- **Version pinning:** [e.g., Exact versions only, no floating ranges]
- **Vulnerability policy:** [e.g., No critical/high CVEs, medium requires justification]

Before adding any dependency, the agent MUST:
1. Verify the package name is correct (check for typosquatting)
2. Check for known vulnerabilities
3. Verify the license is compatible with [license policy]
4. Get user approval

---

## Network Access

<!-- OPTIONAL: Remove if the agent should have no network access -->

- **Authorized external endpoints:** [list specific endpoints or "none"]
- **Authorized internal endpoints:** [list specific endpoints or "none"]
- **TLS requirement:** TLS 1.2+ for all connections
- **Proxy configuration:** [if applicable]

---

## Testing Requirements

- [ ] Unit tests for all new functions
- [ ] Integration tests for [specify what needs integration tests]
- [ ] All tests MUST pass before committing
- [ ] [Language-specific test commands, e.g., `pytest`, `npm test`]

---

## CI/CD Pipeline

- **Branch protection:** [e.g., main requires 1 review, no force push]
- **Required CI checks:** [e.g., lint, test, SAST, SCA, secrets scan]
- **Deployment:** [e.g., Automated to dev, manual approval for prod]

The agent MUST NOT:
- Modify CI/CD configuration without explicit approval
- Skip or bypass any required CI check
- Deploy directly to production

---

## Incident Response

If the agent discovers a potential security vulnerability:
1. Stop the current task
2. Report the finding to the user immediately
3. Do NOT create a public issue for security vulnerabilities
4. Follow [agency incident response procedure reference]

---

## Agent Meta-Constraints

<!-- These constraints govern how the agent operates — plan, verify, and fail safely -->

The agent MUST:
- [ ] Output an execution plan and wait for approval before modifying artifacts
- [ ] Submit all changes via PR with: Context, Plan, Verification, Rollback, Security Impact sections
- [ ] Produce a verification transcript (commands run, outputs, pass/fail) for every change
- [ ] Execute verify → fix → re-verify loops until all checks pass
- [ ] Fail closed on ambiguity — halt and escalate, never guess
- [ ] Not retry failed operations silently — report, diagnose, propose

**Risk modes for this project:**

| Mode | Scope | Requires Approval |
|------|-------|-------------------|
| Read-only | Analyze, review, answer questions | No |
| Scoped edit | Modify files identified in plan | Plan approval |
| Broad refactor | Cross-module changes | Plan + per-module approval |
| Infrastructure | CI/CD, deployment, access control | Explicit per-change approval |

---

## Engineering Discipline

The agent MUST:
- [ ] Create an ADR before: adding dependencies, changing auth, introducing data stores, altering module boundaries
- [ ] Enforce size limits: ≤[50] lines/function, ≤[400] lines/file, ≤[10] cyclomatic complexity
- [ ] Write failing test before production code (TDD: red → green → refactor)
- [ ] Add regression test for every resolved defect
- [ ] Not implement speculative features (YAGNI)
- [ ] Extract shared logic only at 3+ occurrences (Rule of Three)

**One-command bootstrap:** `[make setup / ./scripts/setup.sh / npm run setup]`
**One-command verify:** `[make check / ./scripts/verify.sh / npm test]`

**ADR location:** `[docs/decisions/]`

---

## Contacts

- **Project Lead:** [Name, email]
- **Security Contact:** [Name, email, or security mailing list]
- **ISSO:** [Name, email]

---

<!--
  CUSTOMIZATION NOTES:

  Sections you should always customize:
  - Project Context (language, framework, data classification)
  - Permitted/Prohibited Actions (project-specific boundaries)
  - Data Handling (your specific sensitive data types)
  - Dependencies (your registries and license policy)
  - Engineering Discipline (size limits, bootstrap/verify commands, ADR location)

  Sections you may remove if not applicable:
  - Network Access (if agent should have zero network access)

  Sections you should add based on your project:
  - Database access rules (if applicable)
  - API endpoint restrictions (if the project exposes APIs)
  - Deployment-specific rules (if the agent assists with deployment)
  - Additional ADR trigger conditions specific to your domain

  Remember: This file is a BEHAVIORAL CONTRACT with the AI agent.
  Be specific. Be explicit. Agents follow literal instructions.
-->
